from _xhr_tool._annotate import singleObj
from _xhr_tool._utils.RUtils import tool
@singleObj
class ExcuteLog():
    def excutelog(self,act):
        pass
        # tool().print("正在执行"+act.get('way')+"方法。"+str(act.get('cssStr')))